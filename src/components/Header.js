import React from "react";

const Header = () => {
    return (
        <>
            <h1>Hangman</h1>
            <p>Find the hidden word - Enter a letter</p>
            <small>** Dashes are the only allowed special character.</small>
        </>
    );
};

export default Header;
